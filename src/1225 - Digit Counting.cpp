#include <iostream>

using namespace std;
/*
int main() {
    int n;
    cin >> n;
    while (n--) {
        int num;
        cin >> num;
        int count[10] = {0};
        for (int i = 1; i <= num; i++) {
            int number = i;
            while (number > 0) {
                int digit = number % 10;
                count[digit]++;
                number /= 10;
            }
        }
        for (int i = 0; i < 9; i++) {
            cout << count[i] << " ";
        }
        cout << count[9] << endl;
    }
    return 0;
}
*/