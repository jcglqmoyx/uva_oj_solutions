#include <iostream>

using namespace std;
/*
int main() {
    int n;
    cin >> n;
    while (n--) {
        int score = 0;
        int len = 0;
        string str;
        cin >> str;
        for (int i = 0; i < str.length(); i++) {
            if (str[i] == 'O') {
                len++;
            } else {
                score += len * (len + 1) / 2;
                len = 0;
            }
        }
        score += len * (len + 1) / 2;
        cout << score << endl;
    }
    return 0;
}
 */